export default defineContentConfig({
  // Default collection configuration
  collections: {
    // You can define custom collections here if needed
    // For now, we'll use the default collection
  }
})
